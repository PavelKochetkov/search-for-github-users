import { ReactElement } from "react";

const HomePage = (): ReactElement => {
  return (
    <div className="flex justify-center items-center h-screen">Главная страница</div>
  );
};

export default HomePage;